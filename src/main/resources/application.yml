spring:
  datasource:
    url: jdbc:mysql://43.138.252.15:3306/bm?useSSL=false&serverTimezone=UTC&characterEncoding=utf-8
    username: root
    password: 111716
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.cs.dao
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.cs.mapper: debug

server:
  port: 8081  # 指定端口为8081